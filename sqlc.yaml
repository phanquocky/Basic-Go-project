version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/query/"
    schema: "./db/migration/"
    gen:
      go:
        package: "db"
        out: "./db/sqlc"
    # emit_json_tag: true
    # emit_prepare_queries: true
    # emit_interface: false
    # emit_exact_table_names: false
